# PMCvitan Agent Entry Point

Before architecture or implementation work, read:

1. `docs/superpowers/specs/2026-07-12-modular-construction-control-platform-design.md`
2. The ACTIVE plan: `docs/superpowers/plans/2026-07-20-phase-3-material-readiness.md` — **Phase 3 (Material Readiness pilot) is in PLANNING; the round-1 architecture review returned CONDITIONAL NO-GO with eight findings, all resolved in the plan's §§A–H correction; implementation is BLOCKED until the narrow re-review of those eight decisions passes (Task 1 is then immediately cleared).** The prior plan (`2026-07-15-phase-2-platform-modularization.md`) is **COMPLETE, MERGED AND INDEPENDENTLY CLEARED** (final verdict: **GREEN SIGNAL — PHASE 2 CLEARED**; evidence: `docs/reviews/phase-2-consolidated-review-packet.md`). The platform-kernel spine is **complete and merged** (Tasks 1–7: module boundaries, shared contracts, the canonical audit + `DomainEvent` envelope, the command-execution ledger, the durable transactional outbox, and the PR A/B/C fix-forwards — PR A #161, PR B #162–#164, PR C #165–#166 — all cleared by independent review). Tasks 8–9 (decisions command/query contract #167; rebuildable projections + generation-swap rebuild + module-owned read path #168) are **merged and cleared**. Task 10 module extraction is **four modules in**: daily-log (PR #169 + corrections #170–#173), drawings (PR #174 + corrections #175–#177), inspections (PR #178 + the owned-facts correction rounds #179–#180), and activities (**Module 4, PR #181, merged at `main` `5d24d82`**). The Module-4 gate review's one architectural finding (canonical columns mutated ONLY by `ON DELETE SET NULL` FK actions produced no owner-aligned signal) was fix-forwarded in **PR #182 — reviewed, cleared and merged at `main` `2c4d4ee`** (owner-aligned participants + signal events, the SET NULL coverage table, the idempotent `projection:rebuild` operator repair). Task 10 **finalization is merged** (**PR #183 at `main` `c94aee3`**): the three cross-cutting surfaces (Inbox/Dashboard/Portfolio) proven end-to-end over the module-owned query contracts with every module read in projection mode, the hardened checkpoint-aware operator rebuild, the production cutover runbook (`docs/RUNBOOK.md`), and the consolidated Phase-2 review packet for the **final Phase-2 review stop**. That review returned BLOCKED narrowly with one P1 production upgrade-path finding (a legacy `decisions.inbox` generation holding a non-empty subset of the canonical register, served as authoritative and unrepairable by the operator command); the focused fix-forward correction — the rebuild registry covering ALL FIVE production projections with a complete normalized row-set diagnostic for decisions, the default run + runbook rebuilding all five before module-query reads/outbox mode are enabled, and a reproduce-first live-PG upgrade probe red at `c94aee3` / green at the correction head — is **PR #184, merged at `main` `5505656`** (base `c94aee3`, runtime commit `6e4d105`, PR head `592d11a`; packet §10). The narrow re-review returned **GREEN SIGNAL — PHASE 2 CLEARED**. **Phase 3 planning (Materials) is unblocked**; Phase 3 implementation begins only after its plan has one independent architecture review and explicit approval. Prior phases are complete and historical: Phase 0 (`2026-07-12-phase-0-trust-foundation.md`) cleared at `main` `5d6f08b`; Phase 1 (`2026-07-13-phase-1-existing-pillars.md`) cleared at `main` `cff18c4` with the round-14 **GREEN SIGNAL** (effective runtime head `302b24a`; evidence in `docs/reviews/phase-1-review-packet.md`).
3. `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/TENANCY.md`, `docs/TEMPLATES.md`, and `docs/ROADMAP.md`

Git is the project memory. Revalidate findings against current `main`; do not rely on chat history. One project represents one site. Project operational records never become global. One fact has one canonical owner. Preserve attributable human approvals. Use additive migrations and prove tenant isolation against PostgreSQL.

Before every PR, include the vision-alignment statement and review packet required by the active plan. A task is not complete until its focused tests and `pnpm check` pass.
