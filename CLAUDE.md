# PMCvitan Agent Entry Point

Before architecture or implementation work, read:

1. `docs/superpowers/specs/2026-07-12-modular-construction-control-platform-design.md`
2. The ACTIVE plan: `docs/superpowers/plans/2026-07-20-phase-3-material-readiness.md` — **Phase 3 (Material Readiness pilot) is IN PROGRESS; the plan CLEARED review — GREEN SIGNAL: PHASE 3 PLAN CLEARED** (round-1 §§A–H corrections merged @ `9a84442`; round-2 five mechanical corrections merged @ `88a29fe`) with explicit GO for Task 1. **Task 1 is merged** (PR #189 at `main` `24ee03f`: ProjectCapability pilot activation §D; ActivityRequirement append-only revisions §F; MaterialSpecificationRef with the technical-identity-only shared fingerprint §B; decimal/UOM round-trips; requirement created/revised/cancelled events §G; the two-projects-one-org inertness proof); its independent review returned **BLOCKED NARROWLY** — sound foundation, but procurement must not reference `ActivityRequirement` until four findings are corrected: P1 caller-authored decision provenance (not server-resolved), P1 append-only by convention only (not DB-enforced; cross-project lineage representable; no safe FK target), P1 material-only model (not type-neutral; `requiredBy` TIMESTAMP not DATE; unvalidated `responsibleId`/`createdById`), P2 incomplete shared contracts + missing read policy. **The round-1 correction (PR #190) is merged at `main` `d0897a6`** (ActivityRequirementRoot lineage + append-only triggers; server-resolved `approvedRef`; type-neutral revisions + revision-owned `MaterialRequirementSpec`; DATE `requiredBy` + membership/identity FKs; shared contracts; explicit `requirement.read` policy). **Its narrow re-review returned six further findings, resolved by the round-2 correction (PR #191, merged at `main` `7d3b29d`)**: the decisions-owned IMMUTABLE `DecisionApprovalRevision` register (one row per approve/reapprove in the same transaction; versions monotonic across provable AND unprovable legacy approvals; PG-append-only), spec provenance FK-bound onto that register (label fallback and event-count identity removed from `approvedRef`), the diagnostic-first migration, commit-time material↔spec pairing + single-source UOM, readiness-locked responsible validation, and an immutable `ActivityRequirementRoot`. The #191 narrow re-review found one P1 (the migration's current-only backfill falsely rejected a VALID earlier-version provenance reference as forged), resolved by the round-3 correction (PR #192, merged at `main` `4cc759a`: the backfill amended in place — documented, never silent — to register EVERY uniquely provable approval/reapproval event; the idempotent `20261216000000_phase3_approval_history` migration completing already-applied databases, three-state operator strategy in `docs/RUNBOOK.md` §0; the upgrade-proof reproduction of a requirement pinning `v1/a` before a `v2/b` reapproval surviving VERBATIM; deterministic barrier-controlled member-removal vs requirement-create races in both orderings). **The Task-1 narrow review returned GREEN SIGNAL: PHASE 3 TASK 1 IS CLEARED** (lineage `7d3b29d → 27e461a → 4cc759a`; no P0/P1/P2 findings remain). **Task 2 (procurement) is MERGED** (PR #193 at `main` `697aa18`): §H `Vendor(orgId)`/`ProjectVendor` dual-composite-FK tenancy + the pilot permission matrix, §F transitions through comparison approval (requisition CAS machine, RFQ, quote supersede/expire, non-lowest justification), quote normalization fields, and the §F bound-1 requirement→requisition allocation chain proven under a deterministic barrier race. **Task 3 (purchase orders + delivery commitments) is DELIVERED on a held PR from `main` `697aa18`**: the versioned PO machine with PostgreSQL-frozen line snapshots (spec fingerprint/make/UOM conversion/rate/taxes/landed/`committedAmountBase` — column-frozen triggers), amendment issuing a NEW version that retains the prior snapshot verbatim (`supersedesVersion` lineage), cancel only with zero accepted receipts vs close-short with reason (closed-short keeps only its received portion), `approvedOverage` ONLY at issuance/amendment with reason (DB CHECK), append-only dated `DeliveryPromise` history, `po.*`/`delivery.*` events, the §F bound-2 requisition-line→PO allocation chain under FOR UPDATE + barrier race, and the requisition-line `ordered`↔`open` flip feeding `requisitions.close`. Task 3 merged at `main` `7ca1fc0` (PR #194). **The Tasks 2–3 independent review returned BLOCKED NARROWLY (seven findings: off-spec material recorded as compliant; dimensionally incorrect purchase-UOM arithmetic; incomplete quotes winning as lowest; unsealed commercial evidence/PO provenance; concurrent double-recorded quotes; multiple commitments per PO line; UTC quote expiry). The one focused correction PR from `main` `7ca1fc0` is DELIVERED on a held PR**: match-only selection + the material-only gate, the explicit `purchaseUom`/`purchaseQty`/`conversionToBase` triple with DB-derived base qty and prorated partial-order tax/freight, complete-coverage-only comparison eligibility, PG-sealed quotes/comparisons + the four-FK provenance chain, one recorded quote per (rfq, vendor) and one commitment per PO line, and project-timezone civil expiry via the injected clock — reproduce-first probes red at `7ca1fc0` for all findings. That correction merged (PR #195 at `main` `bffd7c9`); **its narrow re-review CLEARED six findings — only F4 database provenance remained** (two P1s: a PO could reference a DRAFT comparison at PostgreSQL since the provenance FK omitted status; requisition containment was unenforced through quote and PO lines). **The F4-completion correction from `main` `bffd7c9` is DELIVERED on a held PR**: `PurchaseOrder.comparisonStatus` CHECK-pinned to 'approved' joins the five-column provenance FK (a draft-comparison reference is unrepresentable), and immutable denormalized `requisitionId` columns + composite FKs seal every `VendorQuoteLine` and `PurchaseOrderLine` to their parent RFQ/PO requisition (diagnostic-first migration; triggers extended over the new columns; the reviewer's three probes RED at `bffd7c9` → GREEN). That F4 completion merged (PR #196 at `main` `9520cd4`); **the mechanical re-review returned GREEN SIGNAL: PHASE 3 TASKS 2–3 ARE CLEARED at `9520cd4`** (all 51 migrations apply; the three provenance probes pass; correction suite 14/14; Task 2–3 suites 24/24; upgrade proof PASSED; no remaining P0/P1/P2 findings — the Tasks 2–3 review cycle is closed and F1–F7 are not reopened absent a direct Task-4 regression). **Task 4 (inventory) is MERGED (PR #197 at `main` `160702e`)**: the inventory module owns `StockLot` (each received batch freezing the pinned revision's full §B `MaterialSpecificationRef` + PO-line/commitment provenance, all FK-sealed) and the append-only `StockTransaction` ledger — NO current-quantity column exists anywhere; buckets derive by ONE generic fold per stock key `(projectId, storeLocation, stockLotId)`; PostgreSQL CHECKs pin every §C movement equation (receipt→quarantine with PO+commitment provenance, evidence-bearing acceptance/rejection, reasoned adjustments) while append-only + reversal-inverse triggers seal §C rule iii (each row reversible at most once, a reversal is its target's trigger-verified EXACT inverse and is itself irreversible); every command re-derives buckets under the lot `FOR UPDATE` + the readiness lock and REFUSES any negative bucket; receipts are entered in purchase units through the PO's FROZEN `conversionToBase` (exact-6dp refusal) with §F bound 3 (`Σ accepted+quarantined ≤ ordered + approvedOverage`; a rejection FREES headroom) enforced through the transaction-bound `ProcurementParticipant` PO-line lock, which also appends the procurement-owned `receivedQty` received-progress fact + the version's `issued↔partially_received↔completed` status; `executeCommand` now hands `run` its `CommandExecution` id so every ledger row records its source action (§C rule ii — keyed replays append NOTHING); one `stock.transacted` event per row (§G payload); media cited as ledger quality evidence is not deletable (the media-delete tx invokes `InventoryParticipant.assertMediaDisposable`); acceptance-vs-adjustment BOTH orders and both bound-3 race shapes (60/60 → exactly one; 50/50 → both, exactly 100) proven under the deterministic barrier, races 10×/10 green. **Task 5 (store-to-site flows + the §E mismatch resolution) is DELIVERED on a held PR from `main` `160702e`**: seven new ledger movements through the SAME generic fold — `reservation`/`reservation_release` claiming part of a store key's free pool for a NAMED activity (the §C `freeAvailable ≥ qty` guard IS the fold refusal; a release is guarded by the ACTIVITY's scoped fold), `issue` creating the §E-canonical `MaterialIssue` (“an issue is NOT a delivery”) while consuming the activity's reserved portion FIRST through an explicit same-command `reservation_release` row (so `qty ≤ freeAvailable + reservedForThisActivity` holds exactly and another activity's claim is untouchable), `consumption`/`site_return`/`wastage` recorded AGAINST the referenced issue and moving `issuedToActivity` ONLY (the CHECK arms cannot name a store bucket — the §C double-count guard is structural; per-issue custody by the fold restricted to the issue's rows; wastage demands reason + delete-sealed photo evidence, pmc authority), and `transfer` spanning two store keys in ONE row (`storeLocation` source → `toStoreLocation` destination; reservations DO NOT travel — the source-key freeAvailable refusal); the v2 reversal-inverse trigger copies the activity/issue scope VERBATIM and reverses a transfer by swapping the two locations with buckets unchanged, while `stock.reverse` re-checks the SCOPED truths (consumed custody blocks an issue reversal; a consumed reservation blocks a release reversal); reserve/issue validate their activity target through `ActivityParticipant.materialTarget` (a SECOND inventory workflow-participant edge — cycle-exempt, since §G's read edge runs activities → inventory in Task 6); the §E Daily-Log read is inventory's `stock.issues` query (lot identity joined, custody derived — NOTHING copied into daily-log rows; the non-pilot daily-log response shape is byte-identical); `daily-log.resolveMismatch` (pilot-gated, pmc-only) closes exactly ONE `matched:false` observation with an append-only UNIQUE-per-observation `MismatchResolution` register row — the observation is NEVER edited — and clears the activity block through `ActivityParticipant.clearMaterialMismatchBlock` ONLY when no unresolved mismatch remains for the decision, with the stored material gate falling back to `wait` (never a fabricated ok), status derived from recorded work state, and ONE `activity.material_unblocked` signal; new events `issue.recorded`/`mismatch.resolved`/`activity.material_unblocked` (§G, signal-only); keyed issue replays append NOTHING; the two-60-issues-one-100-pool race admits exactly one under the deterministic barrier; 12/12 focused live-PG tests, all tripwire pins advanced (13 inventory commands, 106 mutating routes, 56 dispatch sites). **The next review stop is NOW: Tasks 4+5 are reviewed together per the user's instruction. The GO's three non-blocking guardrails for Tasks 5–6 are recorded in the plan's §A.** **The Tasks 4+5 review returned an integrity-correction directive (four findings — F1 command provenance, F2 receipt/lot provenance, F3 issue canonicity, F4 mismatch resolution — all requiring the DATABASE to enforce the physical-truth invariants the services already enforce). The one focused correction from `main` `b0edc5a` is DELIVERED on a held PR**: `StockTransaction.sourceCommandId` NOT NULL + project-contained composite FK to `CommandExecution` (unkeyed inventory calls now reserve a SERVER one-shot command via `synthesizeKeyWhenAbsent` — keyed replay unchanged); `StockLot` commitment↔PO-line and PO-line↔requirement-pin bound by composite candidate-key FKs, a BEFORE INSERT spec-fidelity trigger (§B copy + single-source base UOM must match the pinned `MaterialRequirementSpec`), and a receipt↔lot composite FK; a partial unique (one `issue` movement per `MaterialIssue`) + a DEFERRED constraint trigger (every `MaterialIssue` has its canonical movement at commit) + a BEFORE INSERT issue-scope trigger (lot/location/activity + canonical-qty match); and BEFORE INSERT/UPDATE triggers making a mismatch resolution require a `matched:false` observation (FOR UPDATE-serialized) and forbidding a resolved observation from reverting to `matched:true`. The forward-only `20261231000000_phase3_t45_integrity_correction` is diagnostic-first (ABORTS with a per-finding count on any pre-existing violation; never invents provenance; operator repair in `docs/RUNBOOK.md §T45`). The reproduce-first `phase3-t45-integrity.test.ts` is RED at `b0edc5a` (10/10 hostile inserts accepted) → GREEN after (11/11, 10 consecutive), and the upgrade-proof now EXECUTES the hostile inserts over a migrated legacy DB (accepts a coherent chain, rejects all seven forgeries). **That integrity correction merged (PR #199 at `main` `7cae97f`); its narrow Codex re-review ACCEPTED the F1–F4 constraints but found three BOUNDARY defects** (P1 idempotency-enforcement bypass — `synthesizeKeyWhenAbsent` slipped a missing client key past `COMMAND_KEY_ENFORCED`; P1 non-executable runbook repairs — §T45 told operators to UPDATE/DELETE/reverse append-only rows PostgreSQL forbids; P2 duplicate issue movements diagnosed only OPAQUELY inside `CREATE UNIQUE INDEX`). **The one focused boundary correction from `main` `7cae97f` is DELIVERED on a held PR**: `executeCommand` refuses ANY missing client key under enforcement BEFORE synthesis is considered (zero side effects); a real operator repair tool (`t45:preflight` + `t45:repair`) runs ONE bounded maintenance transaction that writes durable before-image `T45RepairAction` evidence, disables ONLY the four `*_append_only` triggers by name, applies an EXPLICIT operator plan (never guesses provenance), re-enables+verifies every immutability trigger, re-runs EVERY diagnostic (now including the new **F3.1** duplicate-issue diagnostic with bounded samples), and commits only if clean — else rolls back completely (data, evidence, trigger-toggle); §T45 rewritten (backup→preflight→`_prisma_migrations` classify→repair→`migrate resolve --rolled-back`→deploy→verify); migration `20261231…` is left byte-for-byte unchanged (its checksum is not silently rewritten — the F3.1 gate lives in the enforced preflight, which reports it BEFORE the migration runs). Reproduce-first RED at `7cae97f`→GREEN: the F1 probe (`command-ledger.test.ts`, 11/11) and the executed repair proof (`scripts/t45-repair-proof.sh` PASSED — preflight names every finding incl. F3.1, migrate deploy aborts over violations, a FORCED repair failure rolls back with triggers intact, the explicit repair clears every finding and the correction then deploys cleanly); upgrade-proof adds the F3.1 State-A seal. **That boundary correction merged (PR #200 at `main` `15e2699`); a follow-up mechanical finding — the F3.1 preflight was DOCUMENTED but NOT ENFORCED in the actual Coolify deploy path (`scripts/migrate.sh` ran `prisma migrate deploy` directly) — is corrected on a held PR from `main` `15e2699`**: `scripts/migrate.sh` now runs the preflight (the COMPILED `node dist/platform/t45/t45.cli.js preflight`, never `tsx`) BEFORE `prisma migrate deploy`, failing closed on any finding so Prisma never starts and 20261231 is never recorded as failed; the preflight is schema-aware (`schemaEligible()` + a robust `migrationState()`) — a fresh/empty or pre-Task-5 database is "not applicable" and passes, an eligible database runs the diagnostics, an already-corrected database stays clean, and the P3005 baseline-and-retry path is preserved. The accepted repair engine and migration `20261231` are UNCHANGED. `scripts/t45-production-runner-proof.sh` runs the ACTUAL `migrate.sh` over all six states (fresh, pre-Task-5, clean, corrected, P3005, dirty-F3.1 — the last NAMES F3.1 + both tx ids, aborts before Prisma with 20261231 never recorded, then an explicit repair lets the same runner deploy). **That production-runner correction merged (PR #201 at `main` `dfd1c0a`); the independent review returned Phase 3 Tasks 4–5 CLEARED at `dfd1c0a`.** **Task 6 (canonical material readiness + UI readiness projection) is DELIVERED on a held PR from `main` `dfd1c0a`** (branch `claude/phase3-task6-readiness`): `InventoryService.coverageFor(tx, projectId, requirements)` folds the §C ledger into `ready | at-risk | blocked` per head requirement — covered = reserved-for-this-activity stock of a §B-satisfying fingerprint PLUS issued custody (the guardrail — issuing reserved stock never un-readies), a commitment-covered shortfall is `at-risk`, an uncovered shortfall is `blocked`; §B satisfaction admits the requirement's own fingerprint OR an ACTIVE `ApprovedSubstitution` target (pmc-authored, revoked by stamp; immutable trigger); `activities.start` reads coverage IN-TX under `lockProjectReadiness` with the §A worst-wins map (`ready→ok · at-risk→wait · blocked→fail · not-required→na`), mismatch-first (a stored `gateMaterial==='fail'` wins), and `pmc` override unchanged — the ONE `deriveMaterialReading` function is shared by start/read-bake/projection so live == projection == rebuild by construction; the SIXTH rebuildable projection `activities.material-readiness` (`MaterialReadinessProjection`) is recompute-only (§G — derives NO domain events; a rebuild emits ZERO events + ZERO notifications) and joins `REBUILDABLE_PROJECTIONS` + the runbook (FIVE→SIX) in the same PR; the §A lock-coverage tripwire gains a COMMAND-LEVEL enumeration (20 commands; `defaultDelivery` lock added). Reproduce-first live PG: `phase3-t6-readiness` 8/8 (na/blocked/at-risk/ready/issued-counts/mismatch-first/substitution/§D-inertness), `phase3-t6-start-races` 5/5 (reservation-release, stock-adjustment, requirement-revision, substitution-revocation, issue — each vs `start`, both orderings), `phase3-t6-projection` 3/3 (live==projection==rebuild + zero events; projection lag never changes a start verdict). Gates: `pnpm check` EXIT 0 (API 629/629, web 396/396); integration 485/485 across 54 files; `upgrade-proof.sh` PASSED (both additive migrations apply over the legacy fixture, wrote no rows, all F1–F4 forgery rejections survive); `test:e2e:api:allmodules` 27/27 and `:outbox` 27/27. Two Task-6-attributable harness corrections: `ApprovedSubstitution` (FK→`ActivityRequirementRoot`) added to the seven Phase-3 TRUNCATE lists + both seed wipes, and the init delivery-count pin 28→32 (the readiness projection is the eighth ordered outbox consumer). Packet: `docs/reviews/phase-3-t6-material-readiness-packet.md`. **Task 6 MERGED (PR #202 at `main` `ec595d1`); its independent review returned BLOCKED NARROWLY with four correctness findings in the canonical readiness calculation, and the ONE consolidated correction from `main` `ec595d1` is DELIVERED on a held PR** (branch `claude/phase3-t6-correction`): **F1** (P1 conservation) — `coverageFor` now allocates each activity's reserved+issued supply across its requirements with a max-flow so every physical unit satisfies at MOST one requirement (exact fingerprints + overlapping substitution edges, deterministic); two 100-unit same-spec requirements + 100 stock can no longer both be ready, 200 covers both; **F2** (P1 spec-bound substitutions) — `activeTargets` carries `fromFingerprint` and the loader admits a substitute only while it equals the CURRENT head fingerprint (an A→B approval stops applying after a requirement is revised A→C), plus a diagnostic-first PARTIAL UNIQUE index (`20270103000000_phase3_t6_correction`) on `(projectId,requirementId,fromFingerprint,toFingerprint) WHERE revokedAt IS NULL` so a concurrent duplicate has one winner and revoking it removes the authorization (revoked rows survive as history); **F3** (P2 quantitative inbound) — `coveringCommitments` returns per-pin `{promisedDate, outstanding = ordered − received}` and `coverageFor` accumulates by date, classifying `at-risk` ONLY when cumulative inbound covers the ACTUAL shortfall (a 10-unit commitment does not cover a 100-unit shortfall → blocked), dated at the covering promise; **F4** (P2 lifecycle) — `deliveries.fulfill` takes `lockProjectReadiness`, refuses an unreceived commitment, and emits `delivery.fulfilled`; `pos.closeShort` emits `po.closed_short`; both events join the shared catalog, external-effect catalog, procurement manifest, and the readiness projection's event set (live == projection == rebuild after either), and the §A command-level lock tripwire enumerates both (20→22). Reproduce-first `phase3-t6-correction.test.ts` 10/10 (RED at `ec595d1` → GREEN). Gates: `pnpm check` EXIT 0 (API 631/631, web 396/396); integration 495/495 across 55 files; `upgrade-proof.sh` PASSED; races 10×/10 green; `test:e2e:api:allmodules(:outbox)` 27/27. Packet: `docs/reviews/phase-3-t6-correction-packet.md`. **That correction MERGED (PR #203 at `main` `c910320`); its narrow re-review returned BLOCKED NARROWLY — the original F1–F4 are resolved, but two COMPOSITION defects remained, corrected in ONE held PR from `main` `c910320`** (branch `claude/phase3-t6-correction2`): **finding 1** (P1 partial fulfilment) — `deliveries.fulfill` gated on `receivedQty > 0`, so a 1-unit receipt could terminalize a 100-unit commitment and silently drop the outstanding balance from inbound; FIX: fulfilment requires NO outstanding committed quantity (`receivedQty >= qty`), else it refuses with the outstanding balance (a partial delivery stays committed/revised, or the PO is closed short); **finding 2** (P2 two-stage allocation) — `coverageFor` allocated PHYSICAL stock first and inspected inbound after, so which requirement a shared pool served (an arbitrary requirement-id order) could flip the activity between `blocked` and `at-risk`; FIX: ONE conserved COMBINED-FLOW decision per activity — physical pools connect to EVERY compatible requirement (fungible; reverse residual edges reroute physical off a commitment-covered requirement), inbound is a DEDICATED `source→requirement` edge pinned to its revision; TWO max-flows decide the verdict from ORDER-INVARIANT flow values (physical-only saturates all → `ready`; physical + all inbound saturates → `at-risk`; else `blocked`), the verdict is uniform across the activity (worst-wins reproduces it) so aggregate AND per-requirement verdicts are permutation-invariant, and chronological commitment accumulation dates the at-risk covering date at the EARLIEST promise that saturates the combined network. Reproduce-first probes F5 (order-invariant at-risk, both orderings) + F6 (1/100 and 99/100 refuse, 100/100 fulfils) RED at `c910320` → GREEN; `phase3-t6-correction.test.ts` 12/12 (F4 fulfil-projection probe rewritten to a legal 100/100 fulfil + projection consistency). NO migration (pure service logic). Gates: `pnpm check` EXIT 0 (API 631/631, web 396/396); integration 497/497 across 55 files; `upgrade-proof.sh` PASSED; races 10×/10 green; `test:e2e:api:allmodules(:outbox)` 27/27. Packet: `docs/reviews/phase-3-t6-correction2-packet.md`. **That composition correction MERGED (PR #204: head `d45de9d`, merge `main` `5b7b8c4`); its independent re-review returned GREEN SIGNAL: PHASE 3 TASK 6 IS CLEARED at `5b7b8c4`** (no P0/P1/P2 findings; partial-receipt fulfil refusals at 1/100 & 99/100 with 100/100 success, order-independent combined-flow verdict with conserved physical stock and requirement-bound inbound, chronological covering date; correction suite 12/12 + adjacent suites 28/28, tripwire 26/26, `pnpm check` web 396/396 + API 631/631, all 10 GitHub checks passed — Task 6 is not reopened absent a direct Task-7 regression). **Task 7 (frontend surfaces + pilot acceptance chain + Phase-3 review packet) is DELIVERED on a held PR from `main` `5b7b8c4`** (branch `claude/phase3-task7`): the pilot-gated, module-owned frontend — ONE Materials hub with seven tabbed panels (requirements/procurement/deliveries/inventory/reservations/issues/readiness), each a GREENFIELD module-query read (never in the legacy snapshot, so module-query-only, no XOR flag) with honest load states (loading/unavailable+Retry/stale) and scope guards; the §D capability gate is per-project — the shell now returns `capabilities: string[]` (`materials` iff `CapabilitiesService.isEnabled`), `SCREEN_CAPABILITY` gates the `materials` screen on it, and `loadMaterials()` is a no-op off-pilot, so the hub, its reads and the shortage action are ABSENT on a non-pilot project; the §25 shortage Inbox action (`selectActionItems` → one `material-shortage` item, worst-first, red on hard impact/amber on soft) carries the forecast (`blocked→no-supply`, `at-risk→delays-start` when the covering commitment lands after the planned start else `covered-in-time`); the readiness read (`GET …/activities/material-readiness`, reusing the `requirement.read` policy) folds the SAME `coverageFor` verdict `activities.start` authorizes (`deriveMaterialReading` worst-wins), projected for the UI; the real-browser live-PG acceptance chain requirement→requisition→comparison→PO→commitment→receipt→acceptance→stock→reservation→issue is proven in BOTH capability states (`materials-pilot.spec.ts`: PILOT hub + READY/BLOCKED verdicts + shortage card + §E stock-issues read; INERT non-pilot no-nav + 404 — the pilot enabled via the operator CLI, the sole §D path). NO domain schema, NO migration (read + UI over already-cleared facts). Reproduce-first: `phase3-t7-readiness-read` 5/5, `materials.test` 10/10, `materials-pilot.spec` 4/4 (both sender modes). Gates: `pnpm check` EXIT 0 (API 631/631, web 406/406); integration 502/502 across 56 files; `upgrade-proof.sh` PASSED; `test:e2e:api:allmodules` 31/31 & `:outbox` 31/31 (one outbox run flaked on three timing-sensitive `pillar-chain` inspection steps unrelated to the read-only change; a clean re-run was 31/31). Consolidated Phase-3 packet mapping §25 + the eight design decisions §A–§H to evidence: `docs/reviews/phase-3-consolidated-review-packet.md` — **final Phase-3 review stop; Phase 4 does not begin.** **Task 7 MERGED (PR #205 at `main` `c642da3`); its final Phase-3 independent review returned BLOCKED NARROWLY — PHASE 3 NOT YET CLEARED (Tasks 1–6 remain valid; PR #205 is additive, no rollback), with five findings on the pilot being observational not operational and on readiness correctness. The ONE focused correction from `main` `c642da3` is DELIVERED on a held PR** (branch `claude/phase3-t7-correction`): **F1/F7** — the Materials hub is now OPERATIONAL: it drives the existing Task 4/5/procurement commands (reserve on-hand stock to an activity, issue to site, record consumption, and the shortage corrective that raises a requisition or reserves covering stock) under their existing authority + stable idempotency keys, and the e2e sets up procurement→acceptance as a FIXTURE only then DRIVES reserve → issue → consume in a real browser with a visible BLOCKED→READY transition (non-pilot 404/no-nav proof kept); **F2** — `loadMaterials` gained latest-request ownership (a monotonic token; an older success OR failure that resolves late is dropped, never overwriting a newer result or its load state); **F3** — readiness + shortage TOTALS are now per ACTIVITY (`MaterialReadinessResult.activities`, `summary` counts activities, `shortages` is one per affected activity; the per-requirement rows are supporting detail), because stock is activity-reserved and Task 6 gives each activity ONE verdict; **F4** — the shortage forecast measures the covering delivery against the EARLIEST need date `min(plannedStartDate, requiredBy)`, so a commitment after `requiredBy` is never `covered-in-time` even when it precedes the planned start; **F5** — `foldActivityReservations` folds the §C `fromBucket`/`toBucket` movements (reversals + issues included) with EXACT decimal arithmetic (`lib/decimal.ts`), so a reversed reservation nets to zero and is not shown. NO domain schema, NO migration (read + UI + corrected read projection over already-cleared facts). Reproduce-first probes for all five findings RED at `c642da3` → GREEN: `phase3-t7-readiness-read` 7/7 (F3 + F4 + corrected covered-in-time), `materials.test` (F2 race + operational actions + activity-level shortage), `reservations.test` 5/5 (F5), `materials-pilot.spec` 4/4 (browser-driven reserve→issue→consume, both sender modes). Gates: `pnpm check` EXIT 0 (web 417/417, API 631/631); integration 504/504 across 56 files; `upgrade-proof.sh` PASSED; `test:e2e:api:allmodules` 31/31 & `:outbox` 31/31 (materials-pilot 4/4 both; one legacy run flaked on an UNRELATED `project-scope` browser-history test, clean re-run 31/31). Packet: `docs/reviews/phase-3-t7-correction-packet.md`. **That correction MERGED (PR #206 at `main` `248212b4`); a follow-up directive — finish the operational Materials pilot WITHOUT browser-side multi-command orchestration — is DELIVERED on a held PR from `main` `248212b4`** (branch `claude/phase3-t7-correction2`): `coverMaterialShortage`'s `Promise.all` fan-out and `reserveMaterial` are REPLACED by EXPLICIT single-command user actions over the SERVER's canonical reservation plan. The new capability-gated read `GET …/activities/:activityId/reservation-plan` (`ActivitiesQueryService.reservationPlan` → `InventoryService.reservationCandidatesFor`) resolves current requirements + ACTIVE substitutions + base-UOM compatibility + lot location + free quantity into EXACT reserve `candidates` (each a single `(lot, storeLocation, qty)` command, the shared free pool CONSERVATIVELY decremented so offers never exceed physical free) + the per-requirement `residuals` to requisition — the browser NEVER recreates coverage compatibility from fingerprints. The store's `reserveCandidate`/`raiseRequisition`/`issueMaterial`(storeLocation)/`consumeMaterial` each dispatch ONE command through the durable WRITE-AHEAD outbox with a STABLE idempotency key (reservations keyed by `(lotId, storeLocation, activityId)` per directive; `lib/materialsKeys.ts` shared by store+screen+tests), coalesced while pending (`materialsPending` + disable-while-pending), retried under the SAME key on a lost response, scope-guarded (project-owned `reservationPlans`/`materialsPending` tear down on switch), and reconciled after success AND uncertain failure by a new `flushOutbox` materials hook (clears resolved keys, reloads the bundle + open plans) — so a partial fix is never reported "covered". `foldActivityReservations` now keys by `(activityId, storeLocation)` and carries the location so an issue draws from the reserved store. Reproduce-first probes 1–8: backend `phase3-t7-reservation-plan.test.ts` 6/6 (100+10→reserve 10/residual 90; two 80s share 100 never over-offer; yard-store candidate; substitute eligible / wrong-UOM not), web `materials.test.ts` (double-click coalesce; lost-response same-key retry; terminal-4xx no-hidden-state + refresh; scope-switch no stale toast) + `reservations.test.ts` (store-location fold) 26/26, e2e `materials-pilot.spec.ts` 4/4 (browser cover→reserve candidate→issue→consume BLOCKED→READY; shortage→ONE requisition; §E read; INERT 404) both sender modes, each browser test self-provisioning fresh stock (re-runnable twice consecutively). Gates: `pnpm check` EXIT 0 (web 422/422, API 631/631); integration 510/510 across 57 files; `upgrade-proof.sh` PASSED (no migration); `test:e2e:api:allmodules` 31/31 & `:outbox` 31/31. NO domain schema, NO migration. Packet: `docs/reviews/phase-3-t7-correction2-packet.md`. **This is the narrow Phase-3 re-review stop; Phase 4 does not begin.** The prior plan (`2026-07-15-phase-2-platform-modularization.md`) is **COMPLETE, MERGED AND INDEPENDENTLY CLEARED** (final verdict: **GREEN SIGNAL — PHASE 2 CLEARED**; evidence: `docs/reviews/phase-2-consolidated-review-packet.md`). The platform-kernel spine is **complete and merged** (Tasks 1–7: module boundaries, shared contracts, the canonical audit + `DomainEvent` envelope, the command-execution ledger, the durable transactional outbox, and the PR A/B/C fix-forwards — PR A #161, PR B #162–#164, PR C #165–#166 — all cleared by independent review). Tasks 8–9 (decisions command/query contract #167; rebuildable projections + generation-swap rebuild + module-owned read path #168) are **merged and cleared**. Task 10 module extraction is **four modules in**: daily-log (PR #169 + corrections #170–#173), drawings (PR #174 + corrections #175–#177), inspections (PR #178 + the owned-facts correction rounds #179–#180), and activities (**Module 4, PR #181, merged at `main` `5d24d82`**). The Module-4 gate review's one architectural finding (canonical columns mutated ONLY by `ON DELETE SET NULL` FK actions produced no owner-aligned signal) was fix-forwarded in **PR #182 — reviewed, cleared and merged at `main` `2c4d4ee`** (owner-aligned participants + signal events, the SET NULL coverage table, the idempotent `projection:rebuild` operator repair). Task 10 **finalization is merged** (**PR #183 at `main` `c94aee3`**): the three cross-cutting surfaces (Inbox/Dashboard/Portfolio) proven end-to-end over the module-owned query contracts with every module read in projection mode, the hardened checkpoint-aware operator rebuild, the production cutover runbook (`docs/RUNBOOK.md`), and the consolidated Phase-2 review packet for the **final Phase-2 review stop**. That review returned BLOCKED narrowly with one P1 production upgrade-path finding (a legacy `decisions.inbox` generation holding a non-empty subset of the canonical register, served as authoritative and unrepairable by the operator command); the focused fix-forward correction — the rebuild registry covering ALL FIVE production projections with a complete normalized row-set diagnostic for decisions, the default run + runbook rebuilding all five before module-query reads/outbox mode are enabled, and a reproduce-first live-PG upgrade probe red at `c94aee3` / green at the correction head — is **PR #184, merged at `main` `5505656`** (base `c94aee3`, runtime commit `6e4d105`, PR head `592d11a`; packet §10). The narrow re-review returned **GREEN SIGNAL — PHASE 2 CLEARED**. Prior phases are complete and historical: Phase 0 (`2026-07-12-phase-0-trust-foundation.md`) cleared at `main` `5d6f08b`; Phase 1 (`2026-07-13-phase-1-existing-pillars.md`) cleared at `main` `cff18c4` with the round-14 **GREEN SIGNAL** (effective runtime head `302b24a`; evidence in `docs/reviews/phase-1-review-packet.md`).
3. `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/TENANCY.md`, `docs/TEMPLATES.md`, and `docs/ROADMAP.md`

Git is the project memory. Revalidate findings against current `main`; do not rely on chat history. One project represents one site. Project operational records never become global. One fact has one canonical owner. Preserve attributable human approvals. Use additive migrations and prove tenant isolation against PostgreSQL.

Before every PR, include the vision-alignment statement and review packet required by the active plan. A task is not complete until its focused tests and `pnpm check` pass.
