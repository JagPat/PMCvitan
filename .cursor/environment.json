{
  "name": "Vitan PMC",
  "install": "bash scripts/cloud-agent-install.sh",
  "start": "bash scripts/cloud-agent-start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "bash -lc 'mapfile -t _pre < <(export -p); set -a && [ -f apps/api/.env ] && source apps/api/.env; set +a; for _line in \"${_pre[@]}\"; do eval \"$_line\"; done; pnpm --filter api start'"
    },
    {
      "name": "web",
      "command": "pnpm dev"
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 3000 }
  ]
}
