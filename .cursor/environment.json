{
  "name": "Vitan PMC",
  "install": "bash scripts/cloud-agent-install.sh",
  "start": "bash scripts/cloud-agent-start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "bash -lc 'PRE_DB=\"${DATABASE_URL:-}\"; set -a && source apps/api/.env && set +a; [ -n \"$PRE_DB\" ] && export DATABASE_URL=\"$PRE_DB\"; pnpm --filter api start'"
    },
    {
      "name": "web",
      "command": "pnpm dev"
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 3000 }
  ]
}
