# Vitan PMC — Roadmap

Phased delivery, building forward from the [architecture](./ARCHITECTURE.md). Status reflects the current branch.

| Phase | Scope | Status |
|---|---|---|
| **0 — Foundations** | Monorepo (pnpm workspaces), `packages/shared` (types, tokens, i18n, helpers, seed), tsconfig/lint, docs | ✅ Done |
| **1 — Design system + responsive shell** | Tokens → CSS vars; self-hosted fonts; primitives + lucide icons; responsive nav (bottom tabs ↔ left rail); URL-synced routing; session/role + language; Zustand store + `DataGateway`/local gateway seeded with "Residence at Ambli" | ✅ Done |
| **2 — Core decision loop** | Decision Log + Client Decisions + approve → confirm modal → lock → log update + dashboard count + notification | ✅ Done |
| **3 — Inspections** | Engineer checklist (guarded) → PMC review (photo-forward, reject toggles) → re-inspection tasks + failed-count. **Checklist↔review now wired**: a submitted checklist (+ auto-created closing inspections) enters a **PMC review queue** — the snapshot returns `reviews[]`, the review screen shows a queue selector, and a checklist item's pass/fail maps to a PASS/FAIL review result | ✅ Done |
| **4 — Site schedule + gates** | Activities, planned/actual timeline, 4 gate dots, Start (gate-guarded)/Mark-complete → auto closing inspection | ✅ Done |
| **5 — Daily site log + attendance + offline outbox** | Check-in (GPS/selfie), crew steppers, QR worker check-in, materials + mismatch → block, progress photos, connectivity/queue/flush | ✅ Done (UI + in-store outbox; Dexie/PWA outbox is Phase 8) |
| **6 — Team access + i18n + worker job card** | Who-are-you → phone+OTP / trade picker → mistri home; worker no-login "tap photo" → job card; en/hi/gu live toggle | ✅ Done |
| **7 — Backend build-out** | NestJS + Postgres/Prisma API implementing the snapshot + core mutations (approve→lock, change, start/complete, flag-mismatch, inspection submit/decide, daily-log submit) with server-side locked-decision authority, gate recomputation, audit events + notifications; dev auth + RBAC read-filtering; `apiGateway` bridge (hydrate from snapshot, `VITE_API_URL`-gated); Dockerfiles + seed | 🟡 Slice 1 done — see below |
| **7b — Write-cutover** | Frontend mutations (approve/change, start/complete, flag-mismatch, inspection submit/decide, daily-log submit) routed through `apiGateway` and reconciled from the returned snapshot when `VITE_API_URL` is set; gateway injected into the store by `useApiSync`; local seeded store unchanged as the default/fallback | ✅ Done |
| **7c-realtime** | WebSocket gateway (socket.io): every mutation emits a `changed` signal to the project room; each client refetches its own RBAC-filtered snapshot — approvals/notifications/"live from site" update across users with no refresh. `useApiSync` opens the socket when `VITE_API_URL` is set | ✅ Done |
| **7c-auth** | Real auth backend: email+password accounts (bcrypt), phone OTP (MSG91 v5, dev-stubbed with no provider), worker device tokens. `/auth/login`, `/auth/otp/request`, `/auth/otp/verify`, `/auth/worker/token`. Passwordless dev auth kept for the demo persona switch, gated by `ALLOW_DEV_AUTH`. Demo accounts seeded; RBAC hardened (only PMC/client see pending) | ✅ Backend done |
| **7c-auth (frontend)** | "Who are you → phone → OTP → engineer home" wired to the auth API: a phone-entry step calls `/auth/otp/request` (dev-code hint shown when the server stubs it), the code screen verifies via `/auth/otp/verify` and adopts the returned engineer token as the real session (`useApiSync` uses it over dev auth); worker tap registers a device token. Falls back to the local demo (any 4-digit code) when `VITE_API_URL` is unset; persona switch drops the real session. Email+password login screen still to come | ✅ Done |
| **7c-auth channels (zero-DLT)** | Pluggable sign-in channels so the app can go fully live without DLT: **Telegram Gateway** phone-OTP (`SmsService` priority MSG91 → Telegram → stub — transparent to the existing phone flow), **email OTP** (`EmailService`, SMTP or stub, `/auth/email/*`), and **Google sign-in** (`GoogleAuthService`, `/auth/google`). All resolve via `signInOrProvision`. **Frontend**: an "Email me a code instead" path (email entry → code) and a `GoogleSignInButton` (Google Identity Services, renders only when `VITE_GOOGLE_CLIENT_ID` is set) on the sign-in screen; local-demo fallbacks map email prefixes to roles | ✅ Done |
| **7c-auth lockdown (secure by default)** | Passwordless dev auth (`POST /auth/session` persona switch) is now **off unless explicitly enabled**. API gate: `ALLOW_DEV_AUTH === 'true'` (else 403). Web flag `VITE_ALLOW_DEV_AUTH`: when off, the persona switcher is hidden and the shell is gated behind a full-screen sign-in (`AuthGate`); the gateway is still injected so the sign-in screen reaches the public `/auth/*` endpoints. `signOut` action + Sign-out control. Local demo (no `VITE_API_URL`) keeps dev auth on so the seeded demo still works. **Mechanism shipped; live switch is env vars — turnkey runbook in [`docs/GO_LIVE.md`](./GO_LIVE.md) (mechanism in [`docs/AUTH_LOCKDOWN.md`](./AUTH_LOCKDOWN.md)).** Boot can auto-seed the office accounts (`AUTO_ENSURE_ACCOUNTS=true`) so the dev-auth lockdown can't lock anyone out. Flip to locked once PMC/client have a working real sign-in (seed accounts / live SMTP / Google) | ✅ Backend + frontend done; go-live is ops-only |
| **7c-media** | Media upload backend: additive `Media` model + `StorageService` (S3/R2 when `S3_*` set, else a dev stub that keeps bytes in Postgres and serves them from `GET /media/:id`). `POST /projects/:id/media` (base64) + `GET /media/:id`; 12 MB body limit; realtime `changed` on upload. Provider-swap is env-only (mirrors OTP). Frontend upload/gallery + snapshot wiring (real geo/time-stamped photos replacing placeholder swatches, tap-to-zoom) is the next slice | ✅ Backend done |
| **8-pwa** | Installable PWA: web manifest + a conservative service worker (network-first HTML, cache-first immutable assets, API never intercepted) + offline app shell. Registered in `main.tsx`; nginx serves `/sw.js` no-cache | ✅ Done |
| **8 — Prisma migrations** | Baseline migration (`prisma/migrations/0_init`) captured from the schema; boot runs `prisma migrate deploy` (falls back to `db push` so a deploy never bricks); one-time `migrate resolve --applied 0_init` baselines the existing live DB — see `docs/DEPLOY.md` | ✅ Done |
| **8 — Offline write outbox** | Gateway mutations captured to a persisted outbox when offline and replayed in order on reconnect (reconciled from the snapshot); connectivity banner counts queued ops; demo (no API) keeps optimistic local writes | ✅ Done |
| **8 — Web push (VAPID)** | `PushSubscription` model + `PushService` (dev-stub when no VAPID keys) + subscribe endpoint + public-key endpoint; notification-bearing mutations fan out a push via the realtime gateway; service worker `push`/`notificationclick` handlers; frontend subscribes when permission is granted | ✅ Done |
| **8 — Hardening (rest)** | **CI/CD auto-deploy** (Coolify redeploys both apps on merge to `main`); **media offline queueing** (progress photos captured to the outbox when offline and replayed on reconnect, reconciled from the snapshot); **per-role push targeting** (`notifyProject`/`notifyChanged` take target roles — approvals → PMC/contractor, re-inspections → engineer, mismatches → PMC/contractor — instead of fanning out to all); **`DELETE /media/:id`** (bucket object + row, project-scoped; **pmc/engineer only**) | ✅ Done |
| **10 — Orgs & multi-project (Slices 1–3)** | Multi-tenant: `Org` (account) owns projects; `OrgMembership` (owner/admin/member) + `Membership` (per-project role) — the access grant tokens scope to. **Tenancy isolation**: `JwtGuard` rejects a `:projectId` route that doesn't match the token; `POST /auth/switch` re-scopes to a project you're a member of (verified on prod: same-project 200, cross-project 403). Endpoints: `/me/memberships`, `/me/orgs`, `POST /orgs`, `POST /orgs/:id/projects` (creator→PMC), **team management** (`GET/POST/PATCH/DELETE /projects/:id/members`, PMC/org-admin gated). **Frontend**: a **project switcher** (left rail, `/me/memberships` + `/auth/switch`), a **Team screen** (add/remove members + set roles), create-project, and a runtime active-project (replacing the hardcoded `PROJECT_ID`). **Slice 3 — phases + monitoring**: a `Phase` model (+ nullable `Activity.phaseId`), snapshot `phases[]` with live per-phase rollups, the **Site Schedule grouped by phase** (progress bar + running/blocked/to-start counts, recomputed live), a **`GET /me/portfolio`** cross-project rollup and a **Portfolio** screen (a monitoring card per project + Open-project switch). Non-breaking (nullable `orgId`/`phaseId`, home-project + flat-schedule fallbacks); `ensure-accounts` backfills org/memberships/phases on prod. **Org super-admin**: an org `owner`/`admin` can operate **every project in their org as PMC** even without an explicit membership — `switchProject` issues a PMC token, and `/me/memberships` + `/me/portfolio` list all org projects — so one "admin" role creates projects, builds teams, assigns roles, and runs everything. See `docs/ORGS.md` | ✅ Slices 1–3 + super-admin done |
| **13 — Location spine (site modules: work + inspections + materials)** | Completes the spine across every module so a place shows its **full** picture. **Activities**, **inspections** and **materials** gain a `nodeId` (all additive/nullable FKs `ON DELETE SET NULL`, migration `20260725000000_location_spine_site_modules`), validated by the shared `resolveProjectNode` guard on create/update; a completed activity's auto-created **closing inspection inherits the activity's node**. Snapshot: `activity.nodeId`, `review.nodeId`/`checklist.nodeId`, and a new top-level **`materials[]`** (all deliveries across the project, not just the current day). **Frontend**: the shared **Zone › Room › Object picker** added to the plan-activity, issue-checklist and add-material flows; the **Site Map gains Work and Materials sections** (activities with live status + block reason; deliveries with swatch + mismatch flag), and the drill-down node cards now count all five modules. Inspections stay on the PMC/engineer surfaces (no all-roles Site Map section) to preserve AUTH-02 review-queue gating. Aggregation extended in `lib/locationTree.ts` (`placeContents`). See `docs/DATA_MODEL.md` | ✅ Done |
| **12 — Location spine (photos + drawings + Site Map)** | The location tree becomes the project's **single spatial index** across disciplines. **Photos** and **drawings** gain a `nodeId` (both additive/nullable FKs `ON DELETE SET NULL`, migration `20260720000000_location_spine_media_drawings`): a photo is the *reality* at a place (subtree semantics), a drawing is the *intent* that governs it (**inherit-down** — filed at its natural level, a room shows drawings from itself/ancestors/descendant details). A shared `resolveProjectNode` guard rejects cross-project nodes. New endpoints: `POST /media` & `POST /drawings` take an optional `nodeId`; `PATCH /media/:id/node` (pmc/engineer) and `PATCH /drawings/:id/node` (pmc) **re-file** an item (or unfile with `null`), returning a snapshot. Snapshot adds top-level `photos[]` and `drawing.nodeId`. **Frontend**: a shared **Zone › Room › Object picker** (extracted, inline node creation) added to the Issue-drawing flow and the daily-log photo upload; a drawing's location shows on its register card + a PMC re-file control in the viewer; and a new **Site Map screen** (`places`, all roles) that browses the tree — pick a place (or the whole project) and see its decisions, the drawings that govern it (`here`/`inherited`/`detail` badges), and photos of what's built, so client/architect/site read the same coordinate. Aggregation in `lib/locationTree.ts` (`placeContents`, `ancestorIds`/`subtreeIds`). See `docs/DATA_MODEL.md` | ✅ Done |
| **11 — Location tree (decisions by place)** | Decisions are fed and browsed by **place** rather than a free-text room. A per-project `ProjectNode` tree (**zone → room → object/`element`**, self-referential, `order`ed) that decisions attach to via `Decision.nodeId` (`ON DELETE SET NULL`). Backend `NodesService`/controller (**PMC-only** create/rename/move/delete) enforces the hierarchy: parent-kind rules, same-project parents, cycle-safe moves, and a **delete guard** (can't remove a subtree that still has decisions). Snapshot carries a flat `nodes[]`; the client rebuilds the tree (`lib/locationTree.ts`) and the **Decision Log gains a control bar** — group by **location / room / object / status / flat**, live search, status-filter chips, and collapsible sections with per-group rollups (total/pending/approved/change) and a `Zone › Room › Object` breadcrumb per row. Issuing a decision uses a cascading **Zone › Room › Object picker** with inline "＋ New…" node creation (+ a "Manage locations" tree editor); the **Client's pending list groups by location** too. Additive/nullable migration (`20260715000000_add_location_tree`) — old decisions keep their `room` and fall back to an "Unfiled" group. See `docs/DATA_MODEL.md` | ✅ Done |
| **9 — Drawings register (Slices 1–3)** | Controlled drawings the team builds from: `Drawing` + `DrawingRevision` models (issue → auto-supersede prior revs so the field always sees the current **For Construction** set); `StorageService` extended to PDF/DWG/DXF (reuses the S3-R2/DB-stub seam); `POST /projects/:id/drawings` (issue, per-role push to engineer/contractor), `GET /drawings/rev/:id` (serve), `DELETE /drawings/:id`; snapshot `drawings[]` (each entry with current rev + history). **Frontend**: a **Drawings register** screen grouped by discipline, a viewer (PDF/image inline, DWG download-only + paired PDF), revision history with superseded marking, and a PMC "Issue drawing" flow. **Slice 2 — linkage + acknowledgement + RBAC**: a `DrawingAck` model (per-revision, so a supersede resets it), `POST /drawings/rev/:id/ack` ("building to Rev C" — idempotent, audited, PMC-notified, client refused), snapshot `acks[]` + `ackedByMe`; **RBAC tiers** (PMC issues — non-PMC now 403; engineer/contractor view+ack; client read-only); a **"Building to Rev X"** block in the viewer (who acknowledged + an "I'm building to Rev X" button) and a **schedule→drawing chip** linking each activity to the drawing it builds from. **Slice 3 — offline + presigned**: the service worker (`v2`) runtime-caches drawing/media files (any origin, stale-while-revalidate) so the current set stays viewable in the field offline; **presigned direct-to-bucket uploads** (`POST /drawings/presign`, PMC-only) for large drawings, auto-routed by `issueDrawing` with a base64 fallback (env-only `S3_*`, `StorageService.presignPut`). See `docs/DRAWINGS.md` | ✅ Slices 1–3 done |

### Phase 7 Slice 1 — what's built vs. deferred

**Built** (`apps/api`, NestJS + Prisma): full Prisma schema + seed for "Residence at Ambli"; `GET /projects/:id/snapshot` (RBAC-filtered); mutations for decisions (approve/change), activities (start/complete → closing inspection), inspections (submit/decide → re-inspection), daily-log (submit, flag-mismatch → block). Locked-decision authority, gate-readiness enforcement, audit events and notifications are server-side. Dev auth (`POST /auth/session` → scoped JWT). Frontend `apiGateway` + snapshot hydration, gated by `VITE_API_URL` (default build unchanged). Domain logic unit-tested; typecheck + build green in CI.

**Deferred to 7b**: real authentication (the current auth is passwordless dev auth); routing the frontend's *write* actions through the gateway (reads hydrate today); media upload; realtime. Also: the sandbox has no Postgres, so the DB-backed paths (snapshot query, mutations, seed) compile and are typed but were not run end-to-end here — validate on first deploy (`docs/DEPLOY.md`).

## Known product gaps (carried forward, not bugs)

- ~~**Checklist ↔ Review are decoupled**~~ **Resolved.** A submitted checklist now feeds the **PMC review queue**: the snapshot exposes `reviews[]` (every submitted-but-undecided inspection — a submitted checklist, the seeded review, and auto-created closing inspections), the review screen renders a queue selector, and each checklist `state` (pass/fail/na) maps to a review `result` (PASS/FAIL). The demo mirrors this locally (submit as engineer → it appears in the PMC's queue).
- **Swatches are CSS-placeholder gradients** standing in for material samples in the decision options. Real *site* photos are now first-class (progress-photo upload/gallery + geo/time stamp + tap-to-zoom, `7c-media`); the decision-option swatches remain as material-sample stand-ins.
- **Persona switcher** ("Viewing as") is now a dev-only device: gated behind `ALLOW_DEV_AUTH`/`VITE_ALLOW_DEV_AUTH` and hidden in the locked-down build, replaced by real sign-in + a full-screen auth gate (`7c-auth lockdown`, `docs/AUTH_LOCKDOWN.md`).
- **Display ids (`DL-…`, `ACT-…`, `INSP-…`) are global database primary keys** (DATA-01). Interim fix in place: the id sequence scans the whole table, so two projects never collide — but numbering is shared across projects (project B's first decision may be `DL-016`, not `DL-001`), and two *concurrent* creates can still race to the same next id and surface a `P2002`/500 (rare; retry succeeds). The durable fix is a schema migration: internal CUID primary key + a per-project display `code` with `@@unique([projectId, code])` allocated in a transaction, remapping every foreign key (`DecisionOption`, `Activity.decisionId`, `SiteMaterial.decisionId`, `Drawing.decisionId/activityId`, `InspectionItem`, media links) and every API lookup. Do it against a staging copy first — it rewrites live primary keys.

## Security follow-ups (tracked from the third-pass audit — not yet fixed)

These need larger, coordinated changes than a point fix; they are called out so they aren't lost. The immediately-exploitable findings from the same audit (OTP code exposure, worker-token PII reads, dev-auth in prod, demo-password provisioning, push SSRF, inspection state-machine, upload MIME/size) are **fixed**.

- ~~**Private, authorized file delivery (media & drawings).**~~ **Resolved.** `GET /media/:id` and `GET /drawings/rev/:id` now require a short-lived `?t=` token (`SignedUrlService`, HMAC over `kind:id:exp`, default 60 min via `FILE_URL_TTL_SEC`) minted only while building the RBAC-filtered snapshot / on upload — a bad/missing token is `403`. Public bucket URLs are never persisted or emitted; in S3/R2 mode the serve endpoint presigns a short-lived GET on demand, so the **bucket must be private** (no public-read). `Cache-Control` dropped from a year to `private, max-age=<ttl>`. A leaked file URL now expires quickly and a revoked user can't mint fresh tokens. Frontend resolves the signed API-relative paths against the API base (`resolveMediaUrl`/`resolveDrawingUrl`).
- **Authenticated realtime (socket.io).** `handleJoin` lets any socket join any `project:<id>` room with no token check; the emitted payload is only a `{ projectId }` "refetch" signal (no data), so the exposure is limited to change-timing for a known project id. Fix: authenticate the socket handshake with the JWT and authorize the room join against the token's project. Low data-exposure, deferred behind the file-delivery work.
- **Outbox replay scope-generation (WEB-02 deepening).** The offline queue is keyed by user+project, but a flush that begins before an identity/project switch can still resolve against the new scope, and the one-time legacy-queue migration is claimed by whoever hydrates first. Fix: stamp a scope generation on each op and abort/ignore a flush whose generation no longer matches the active session. Client-only, shared-browser edge.
- **CORS & JWT are fail-soft by design.** With `CORS_ORIGINS` unset in prod the API reflects the request origin (safe for a bearer-token, cookieless API, but permissive); with `JWT_SECRET` unset it derives a stable secret from `DATABASE_URL`. Both degrade safely rather than failing closed. Set `CORS_ORIGINS` and `JWT_SECRET` explicitly in prod (see GO_LIVE). A future hardening could make these fail-closed behind an explicit opt-out.
- **No per-request token revocation.** A JWT stays valid up to its 12h expiry after a role change or removal; access-resolution changes only affect *newly issued* tokens. Acceptable for now (short expiry, no refresh tokens); a deny-list or short-lived-access-token + refresh design is the durable answer.
- **Presigned drawing uploads have no server-enforced size cap.** The `/drawings/presign` PUT URL lets a PMC upload directly to the bucket; the API caps the *reported* `sizeBytes` on issue but the S3 PutObject presign itself carries no `content-length-range` condition, so a PMC could PUT an oversized object. Low risk (PMC-only, trusted), but the durable fix is a POST-policy upload with a size condition + a server-created upload-session record the `issue` call must match (rather than trusting the echoed `storageKey`; the key is now at least bound to the caller's project prefix).

## Verify locally

```bash
pnpm install
pnpm --filter web dev         # http://localhost:5173
pnpm --filter web test        # Vitest: helpers + core-loop selectors/reducers
pnpm --filter web test:e2e    # Playwright: approve→lock→dashboard, offline flow
pnpm --filter web build       # tsc -b && vite build
```
